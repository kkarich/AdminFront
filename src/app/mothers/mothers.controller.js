(function() {
  'use strict';

  angular
    .module('storkcharts')
    .controller('MothersController', MainController);

  /** @ngInject */
  function MainController($timeout, webDevTec, toastr) {
    var vm = this;
  }
})();
